spring:
  cloud:
    config:
      discovery:
        service-id: Config-server
      uri: http://localhost:8999
      profile: development
  profiles:
    active:
    - development

management:
  endpoints:
    web:
      exposure:
        include: '*'     